My VIM Config
=============

This repo contains my vim config.

To compile all plugins: 
    
    ./gradlew compile

To link the vim,vimrc into the home dir:

    ./gradlew link

To both at once:

    ./gradlew install



